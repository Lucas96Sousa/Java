rules:
  - id: is-comparison
    languages:
      - java
    message: The operator 'is' is for reference equality, not value equality! Use
      `==` instead!
    pattern: $SOMEVAR is "..."
    severity: ERROR

